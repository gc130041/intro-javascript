const texto = prompt('Ingresa una cadena de texto');

if (texto.length > 5) {
    console.log('La cadena tiene más de 5 caracteres.');
} else {
    console.log('La cadena no tiene más de 5 caracteres.');
}